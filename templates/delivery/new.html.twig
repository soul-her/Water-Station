{% extends 'base.html.twig' %}

{% block body %}
<div class="max-w-md mx-auto mt-10 p-8 bg-white rounded-xl shadow">
  <h2 class="text-2xl font-bold mb-4 text-center">🛒 Place Your Order (COD Only)</h2>

  {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="mt-4 px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700">Add to Cart & Place Order</button>
  {{ form_end(form) }}
</div>
{% endblock %}
